# URL of the Flask backend (no trailing slash).
# In development this is http://localhost:5001
# In production set this to your Railway backend URL, e.g.:
#   https://stockroom-api-production.up.railway.app
REACT_APP_API_URL=http://localhost:5001
